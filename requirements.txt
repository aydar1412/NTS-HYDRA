# Обязательные зависимости расчёта
numpy>=1.22
pandas>=1.4
matplotlib>=3.5
openpyxl>=3.0

# Необязательно: нужно только для сборки руководства (tools/make_manual.py),
# иллюстрации в нём берутся из готового PDF-отчёта.
# ВНИМАНИЕ: PyMuPDF распространяется под AGPL-3.0. Для расчёта он не нужен.
# pymupdf>=1.23
